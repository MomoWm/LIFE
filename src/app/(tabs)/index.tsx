import { PlaceholderScreen } from '@/components/ui/placeholder-screen';

export default function TodayScreen() {
  return (
    <PlaceholderScreen
      title="Today"
      note="Your time-of-day-aware dashboard — 545, prayer, workout, and work — lands in Phase 8."
    />
  );
}
